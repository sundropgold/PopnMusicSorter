$(document).on(ready, function(){

	// array of characters unsorted
	var popnUnsorted = [];
	var popnUnsortedImg = [];

	// array of characters sorted
	var popnSorted = [];
	var popnSortedImg = [];

	// while characters are not yet sorted, continue looping

	// keep track of battle numbers

	// how much percent sorted

	// picture/button one = +1 point

	// picture/button two = +1 point

	// love them both = +1 point to both

	// hate them both = -1 point to both

	// neutral = nothing happens

	// table of results
	// rank - character img/name - number of points
});